export interface IFeature {
  id: string;
  title: string;
}
export interface IInput {
  id: string;
  name: "email";
  label: string;
  type: string;
}
export interface IInputsValues {
  email: string;
}
